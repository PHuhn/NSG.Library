{
  "connectionStrings": {
    "NetworkIncidentConnection": "Data Source=.\\Express; Initial Catalog=NetIncident2; Integrated Security=SSPI; MultipleActiveResultSets=true;providerName=System.Data.SqlClient;"
  },
  "Email": {
    // SMTP host name or IP-address
    "SmtpHost": "localhost",
    // for further SMTP configuration, see mailSettings
    // The <api key> for either SendGrid or Mailgun
    "ApiKey": "SG.11111111111",
    // The Mailgun requires both ApiKey and this Domain name
    "MailgunDomain": "MG.Domain"
  }
}
